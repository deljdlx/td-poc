/**
 * GameState - Enum for game states
 */
export const GameState = Object.freeze({
    READY: 'ready',
    RUNNING: 'running',
    PAUSED: 'paused',
    GAME_OVER: 'game_over',
    VICTORY: 'victory'
});
